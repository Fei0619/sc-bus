spring:
  application:
    name: BUS-CORE
  profiles:
    active: local
server:
  port: 9001

mybatis:
  # mapper文件路径
  mapper-locations: classpath:mapper/*.xml
  configuration:
    # 驼峰自动映射
    map-underscore-to-camel-case: true